datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Student {
  id         String   @id @default(cuid())
  name       String
  surname    String
  email      String
  age        Int
  created_at DateTime
  updated_at DateTime
}
